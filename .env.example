# Required: Telegram bot token from BotFather
TG_BOT_TOKEN=

# Required: comma-separated Telegram IDs allowed to use bot
ALLOWED_TG_IDS=

# Required: PostgreSQL connection
DB_USERNAME=clawd_bot
DB_PASSWORD=
DB_HOST=127.0.0.1
DB_PORT=5432
DB_NAME=clawd_bot

# Optional: local sqlite mode for quick dev
# LOCAL=True

# Optional: startup notifications (comma/semicolon-separated tg ids)
SERVICE_ACCOUNTS=

# Optional: keep empty for long polling
WEBHOOK_URL=
WEBHOOK_SECRET_TOKEN=

# Optional: voice recognition model/language
WHISPER_MODEL=small
WHISPER_LANGUAGE=ru

# Optional MAX platform integration (not needed for Telegram-only setup)
MAX_BOT_TOKEN=
MAX_API_BASE=https://platform-api.max.ru
MAX_POLL_TIMEOUT=30
WEBHOOK_MAX_URL=
WEBHOOK_MAX_SECRET=
MAX_WEBHOOK_PORT=18003
